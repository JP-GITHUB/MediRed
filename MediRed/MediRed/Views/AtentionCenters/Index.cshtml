@model IEnumerable<MediRed.Models.AtentionCenter>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Address)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PhoneCener)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Address)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PhoneCener)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AtentionCenterId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AtentionCenterId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AtentionCenterId })
        </td>
    </tr>
}

</table>
